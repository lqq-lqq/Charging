<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.charging">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Charging">
        <!--<activity android:name=".cityChoice.activity.city_choice"></activity>-->
        <activity
            android:name=".cityChoice.activity.city_choice"
            android:label="city_choice"
            android:theme="@style/Theme.Charging.NoActionBar" />
        <activity android:name=".mainActivity.MainLayoutActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>